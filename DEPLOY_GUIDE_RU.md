# 🚀 Быстрый гайд по деплою

## Самый простой способ (Debug APK)

### 1. Запушьте код на GitHub
```bash
git add .
git commit -m "Custom browser modifications"
git push origin main
```

### 2. Скачайте APK
1. Откройте ваш репозиторий на GitHub
2. Перейдите во вкладку **Actions**
3. Дождитесь завершения сборки (зеленая галочка ✅)
4. Кликните на успешный запуск
5. Скачайте артефакт **custom-browser-debug**
6. Распакуйте ZIP → установите APK на телефон

**Готово!** APK собран и готов к установке.

---

## Создание релиза (Release APK с подписью)

### Шаг 1: Создайте ключ для подписи

```bash
keytool -genkey -v -keystore my-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-alias
```

Запомните:
- Пароль keystore
- Пароль ключа
- Алиас (например, `my-alias`)

### Шаг 2: Конвертируйте в base64

**Linux/Mac:**
```bash
base64 my-key.jks | tr -d '\n' > keystore.txt
```

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("my-key.jks")) > keystore.txt
```

### Шаг 3: Добавьте секреты в GitHub

1. Репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **New repository secret**
3. Добавьте 4 секрета:

| Имя | Значение |
|-----|----------|
| `SIGNING_KEY` | Содержимое `keystore.txt` |
| `KEY_ALIAS` | `my-alias` |
| `KEY_STORE_PASSWORD` | Ваш пароль keystore |
| `KEY_PASSWORD` | Ваш пароль ключа |

### Шаг 4: Создайте релиз

```bash
git tag v1.0.0
git push origin v1.0.0
```

**Автоматически:**
- ✅ Соберется APK
- ✅ Подпишется вашим ключом
- ✅ Создастся GitHub Release
- ✅ APK прикрепится к релизу

### Шаг 5: Скачайте релиз

1. Репозиторий → **Releases**
2. Скачайте `custom-browser-v1.0.0.apk`
3. Установите на телефон

---

## Ручной запуск сборки

1. GitHub → **Actions**
2. Выберите workflow:
   - `Build Custom Debug APK` - для debug версии
   - `Build Custom Release APK` - для release версии
3. Нажмите **Run workflow**
4. Выберите ветку → **Run workflow**

---

## Что делать если...

### ❌ Сборка упала
- Проверьте логи в Actions
- Убедитесь что код компилируется локально: `./gradlew assembleInternalDebug`

### ❌ APK не устанавливается
- Разрешите установку из неизвестных источников в настройках Android
- Для release: проверьте правильность подписи

### ❌ Нет секретов для подписи
- Используйте debug версию (она работает без подписи)
- Или настройте секреты по инструкции выше

---

## Локальная сборка (без GitHub)

Если не хотите использовать GitHub Actions:

```bash
# Debug
./gradlew assembleInternalDebug

# APK будет здесь:
# app/build/outputs/apk/internal/debug/
```

---

## Структура workflows

```
.github/workflows/
├── build-custom-debug.yml    ← Автоматическая debug сборка
└── build-custom-release.yml  ← Release сборка с подписью
```

**Debug workflow** запускается при каждом push в `main` или `develop`

**Release workflow** запускается при создании тега `v*` (например, `v1.0.0`)

---

## Рекомендации

✅ **Для тестирования:** Используйте debug версию  
✅ **Для публикации:** Используйте release версию с подписью  
✅ **Версии:** Используйте формат `v1.0.0`, `v1.1.0`, `v2.0.0`

---

## Полная документация

Подробная инструкция: `GITHUB_ACTIONS_SETUP.md`
